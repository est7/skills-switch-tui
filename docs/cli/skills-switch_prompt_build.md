## skills-switch prompt build

Build a generated system prompt from its source files

```
skills-switch prompt build <group> [flags]
```

### Options

```
  -h, --help   help for build
      --json   emit JSON
```

### Options inherited from parent commands

```
      --lang string        interface language: auto, en, or zh (default "en")
      --project string     project directory (default current directory)
      --resources string   agent resources root (default ~/.agents/resources)
```

### SEE ALSO

* [skills-switch prompt](skills-switch_prompt.md)	 - Manage user-global system prompt files

