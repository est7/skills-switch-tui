## skills-switch commands enable

Enable a project resource for selected clients

```
skills-switch commands enable <resource-id> [flags]
```

### Options

```
      --client strings   registered target client (repeatable)
  -h, --help             help for enable
      --json             emit JSON
```

### Options inherited from parent commands

```
      --lang string        interface language: auto, en, or zh (default "en")
      --project string     project directory (default current directory)
      --resources string   agent resources root (default ~/.agents/resources)
```

### SEE ALSO

* [skills-switch commands](skills-switch_commands.md)	 - Manage project command files

